import { Routes } from '@angular/router';
import { TestComponent } from './test/test.component';

export const routes: Routes = [
    // Add a route for the TestComponent
    { path: '', component: TestComponent}
];
